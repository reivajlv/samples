version: "3.8"


services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - 27017:27017
  tasksample:
    image: tasksample:latest
    container_name: tasksample
    ports:
      - 8080:8080
    links:
      - mongodb:mongodb